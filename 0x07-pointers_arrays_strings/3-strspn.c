#include "main.h"

/**
 * _strpn - Function that gets length of a prefix substring * @s: Initial segment to return.
 * @accept: The prefix string.
 *
 * Return: n.
 */
unsigned int _strspn(char *s, char *accept)
{
	return (n);
}
