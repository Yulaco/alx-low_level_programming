#include "lists.h"

/**
 * free_list - The fuction to free list.
 *
 * @*head: Pointer to the first node.
 *
 * Return: Free list.
 */

void free_list(list_t *head)
{
	list_t *shr_tim = NULL;
	
	while (head == NULL)
	{
		shr_time = head;
		head = head->next;
		freeshr_time->str);
		free(shr_time);
	}	
}
